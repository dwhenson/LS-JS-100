// What does the following code log to the console?

function scream(words) {
  words = words + "!!!!";
  console.log(words);
}

scream("Yipeee");

// Nothing as the return statement stops everything - remove 'return' before console.log()
