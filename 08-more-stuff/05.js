// What does the following function do?

function doSomething(string) {
  return string
    .split(" ")
    .reverse()
    .map((value) => value.length);
}

// Tales and a string, turns it into an array splitting on each space, reverses
// the order and returns the lenght of each work
